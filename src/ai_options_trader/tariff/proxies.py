from __future__ import annotations

# v0: placeholders to get the engine running end-to-end.
# We will replace these with tariff-relevant series after validation.

DEFAULT_COST_PROXY_SERIES = {
    # column_name: FRED_SERIES_ID
    "COST_PROXY_V0": "PPIACO",  # PPI: All Commodities (broad cost pressure proxy)
}
