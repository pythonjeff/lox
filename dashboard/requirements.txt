# Dashboard requirements - uses the main package
# Install main package first: pip install -e ..
# Then install these dashboard-specific dependencies

# Web framework
flask>=3.0.0
gunicorn>=21.0.0

# Note: Other dependencies (pandas, requests, openai, alpaca-py, pydantic, etc.)
# are inherited from the main package installation
